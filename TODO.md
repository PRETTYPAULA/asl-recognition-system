## TODO

- [ ] Fix `static/js/detection.js` target-letter practice flow so it consistently cycles A→Z
  - [ ] Remove duplicated/contradicting blocks (duplicate finishedKey/finished + duplicate initial target display)
  - [ ] Ensure `currentTarget` is initialized once and UI is updated once
  - [ ] Keep uppercase display and 10s delay before advancing to next letter
- [ ] Run server and verify detection page behavior end-to-end

